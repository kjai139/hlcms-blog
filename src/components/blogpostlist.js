import { graphql, useStaticQuery } from 'gatsby'
import * as React from 'react'


const BlogPostList = ({ skip, limit}) => {
    
    
    

    return (
        <div>
            blogpostList
        </div>
    )
}

